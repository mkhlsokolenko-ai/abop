# Session summary (обновляется каждые ~2 часа)

_Обновлено: 2026-09-14 (8). Ветка `feat/data-plane-agents-deploy`, git **ЧИСТ** (кроме untracked-summary), HEAD `1232c01` (БД-фаза: рецепты/коннекторы→PG). Прод :8091 пересобран — задеплоено (проверено: backfill 9 файловых рецептов→PG, сохранение ПЕРЕЖИЛО рестарт, audit1c-рецепты резолвятся из PG). **Ветка НЕ смёржена в main и НЕ запушена.**_

## Инфра / деплой / правила
- **Сервер-1** `5.129.192.63`: ABOP :8091 (`abop-webapi`, host-сеть, volume `abop_ape`, `--env-file /opt/abop/.env`, TRUNCATE=0) + BookStack :6875/Mailpit :1025,8025/Redmine :3000/Twenty :3002/NocoDB :8090/Gitea :3001/MinIO :9000/kroki :8000. `/opt/abop`=снапшот репо.
- **Сервер-2** `201.51.5.24`: 1С + sLAVA/Qdrant + Gotenberg :3050. socat `slava-relay.service`.
- **Деплой**: `git archive HEAD cli server skills webapp demo -o _deploy.tar` → scp `/opt/abop` → `tar -xf` → `docker build -f Dockerfile.webapi -t abop-webapi .` → `docker rm -f`+`docker run`. NB: если ssh падает ДО tar-xf → образ из старых файлов, распаковать+пересобрать.
- **Бандл** `webapp/index.html`=1 JSON-строка (~384К). decode→replace→`json.dumps`+`.replace("</","<\\u002F")`→self-check. Gzip: GraphLens `adcd913c` (канва+ИНСПЕКТОР), OpsLens `82ed70ed`, AdminScreens `fbdc0993`. Репак: extract→gunzip→replace→gzip+b64→roundtrip. **Инспектор узла рендерится в gzip GraphLens** (execMetrics/selFamily/selIsSource — пропы из осн.шаблона).
- **УРОКИ**: `self`=window.self в браузере → в computed ТОЛЬКО стрелки+`this`. index.html `Cache-Control: no-cache`. SSH/scp через PowerShell; проверки — .sh/.py файлом (inline-python искажается). SVG поверх кнопок перехватывает клики → `pointer-events:none`. Правки бандла ВСЕГДА писать через `python3 - << 'PYEOF'` heredoc (single-quote) с `.replace("</","<"+chr(92)+"u002F")`.
- Gitignored: `_*.py/.sh/.txt/.json/.html`(врем.), `_1c_*`, `_deploy.tar`. RouteAI/Keycloak креды в .env.

## КЕЙС «1С-Аудитор» — end-to-end, задеплоен (демо ~2026-09-26)
Контракт `audit1c-holding-2026`, агент `.v1` (семья **audit**/auditor-1c, A1, 7 навыков). doc1c(107)/ref1c(50), sLAVA `slava_audit1c_norms`, вывод Gotenberg→BookStack→Mailpit. 4 линзы+петля. Прогон реальный (/api/runs run_live ~4мин + audit1c_run_checks → 10 находок A1/B6/C1/D2).

## СДЕЛАНО за сессию (всё задеплоено)
Ядро: лого, семья Аудит+агент+контракт+петля, де-фин Прогона, канва (layoutFlow+PG-координаты+рёбра #16), Строю (модалка реального прогона+пайплайн), линзы, Конверт 3-секц, граф данных mind-map, журнал #17, Безопасность #15 (Аудит ИБ+RBAC реальные), Live #18.
**Доработки владельца (`Desktop/Доработки фронта АБОП..txt`) — СДЕЛАНО:** ✅2.3 канва вход · ✅5.1 граф-клик+цепочка+undo · ✅5.2 убраны Карточки · ✅6.1 версии не плодятся (save_draft/revise) · ✅2.2 инспектор реальный (safety навыка) + **настройка входа данных** (селектор рецепта у source-узла, «Открыть рецепт»→конкретный, навыки роли) · ✅3.1 Прогон HITL динамический · ✅2.4/2.5/2.6 палитра (убраны вкладки Роли/Рецепты + хардкод-группа «Аудит 1С»; инспектор роли из реального familiesList) · ✅1.1 Обзор убран «Последние».

## ⏳ ОСТАТОК доработок владельца
- **4.1 — СДЕЛАНО (локально проверено playwright, НЕ задеплоено):** Конверт: убран хардкод-блок «Автономия против контракта LUDA» (фейк-прогрессбар, «L2≤L3✓», хардкод HITL/резидентности) → новая карта **«Настройки агента · правьте по клику»**: по каждому навыку из реального AgentVersion — тег автономии (клик циклит ≤ потолка контракта) + тег HITL (клик переключает; внешнее действие закреплено под HITL ADR-014); режим/cite из каталога навыков; сводка «автономия ≤ контракт»; кнопка **Пересмотр → новая версия** (строит граф из pp.nodes+черновик, POST revise=true → реальный v+1) + «Сбросить правки»; реальные факты (навыки/точки HITL/резидентность=egress). Верхняя колонка РАЗРЕШЕНИЯ (envAutoChips) = bulk на весь черновик, консистентно. loadPassport теперь стор nodes/edges; applyGo+openEnvelope зовут loadPassport при входе в линзу. **Побочно исправлен латентный баг:** `{{ reviseEnvelope }}`/`{{ resetEnvDraft }}` не были computed-пропами (кнопка «Пересмотр» была мёртвой) → добавлены `reviseEnvelope/resetEnvDraft: () => this.…()`. Проверено E2E: клик по тегам меняет значения, dirty-индикатор, Пересмотр создаёт audit1c-holding-2026.v2 с точными правками (extract→A0+HITL). **Осталось по Конверту (вне 4.1, хардкод):** prod-статус-бар (passVersion из deployments d1), Flow-схема, Аудит-след («182k токенов»/auditRows), passportChips — на будущее.
- **7.1** Биллинг — хардкод везде (1 240₽/0.42₽/@ivan…) → реальный (RouteAI считает токены/деньги). **7.2** Админ-панель (модели/арендаторы/подключения) переработать почти полностью, гриллить.
- **Прогон HITL «Предпросмотр» — СДЕЛАНО (задеплоено `54ae828`):** модалка HITL (gzip Overlays `039d0e6e`) больше не выдумывает письмо (zakazchik/маркетплейс/NPV) — контент сценарно-консистентный из реальных данных: `hitlTitle`/`hitlMeta` + `hitlPvBody` из `s.lastRun.findings_summary` (audit1c: N находок A/B/C/D; нет прогона → честный пустой предпросмотр), `hitlPvProv` (doc1c/ref1c/sLAVA), `hitlApproveLabel`. Проверено E2E.
- **КАНВА-баг2 — СДЕЛАНО (задеплоено `6031b67`):** причина — `fitSurface` (⤢ разворот/сворот + resize) ПЕРЕСЧИТЫВАЛ координаты узлов `sx=(w-220)/(oldW-220)` → цепочку ~2600px ужимал в ~700px (узлы 208px наезжали), повторные тоглы+клэмп копили сжатие. Фикс: fitSurface больше НЕ трогает координаты — сайзит surface под bbox узлов и считает fit-зум прямо из размеров окна. Проверено E2E: координаты стабильны через тоглы, полный экран = 45%, вся цепочка в кадре.

## ⛔ БЕКЛОГ — ПЕРСИСТЕНТНОСТЬ В БД + СНОС ХАРДКОДА (в работе)
Директива владельца 2026-09-14 (собрано «подбери все»): **все правки пользователя в ЛЮБОМ месте → в Postgres, не в localStorage** (иначе per-браузер, не мультиюзер, конфликт с БД, теряются при перенакате). **Весь хардкод убрать — только реальные данные.** Полный аудит+инвентарь → [[persistence-localstorage-hole]].
- **✅ Навыки → PG (СДЕЛАНО, задеплоено `bddb712`):** `server/skill_store.py` (PG+mem); `POST /api/skills/{sid}` (правки навыка новой версией, редактор=реальный юзер), datasources перенесены с файла на PG; GET оверлеит правки; убраны `skillList/skillMeta/skillEdits` из PERSIST_KEYS (снято затенение); `commitSkill`→сервер; убран хардкод `@sokolenko`; ape инжектит data-need из PG. **Проверено: правка пережила рестарт контейнера.** Governance-безопасность остаётся авторитетной из каталога .md.
- **✅ Рецепты/коннекторы Data Plane → PG (СДЕЛАНО, задеплоено `1232c01`):** `server/dataplane_store.py` (dp_recipes/dp_connectors); ape-инъекция `set_recipe_store/set_connector_store`; POST рецепты/коннекторы/rebind→PG; одноразовый backfill `~/.ape`→PG на старте (демо-рецепты сохранены); убраны `recipesList/connectorsList/connState` из PERSIST_KEYS. **Проверено: сохранение пережило рестарт, audit1c-рецепты резолвятся из PG.**
- **A. Остальная персистентность (❌ только LS):** админка модели/арендаторы/квоты (7.1/7.2), роли/пороги, флот, userScenarios/memory. Корень: `persist()` пишет `PERSIST_KEYS` в `localStorage['abop.proto.v1']` и затеняет серверные данные. ✅ PG: agents/layout/contracts/runs/**skills**/**рецепты**/**коннекторы**. Клиентские сиды-хардкоды (`const SKILLS`, `recipesList/connectorsList` init) — заменяются серверными на load, но убрать физически (follow-up).
- **B. Хардкод-инвентарь:** деньги (0.42₽/1 240₽ из 4 000₽/31%/0.14₽/escThresholds/quotaLimit=7.1); Конверт-хвост (`auditRows`/`182k токенов`/`passportChips`/`recentRuns`/`flowNodes`/prod-бар `passVersion`); клиентский `const SKILLS`; фейк-@хэндлы `@ivan/@ivanov/@olga/@petrova/@security/@sokolenko/@zakazchik`; флот `deployments`/`instances`; Прогон HITL «Предпросмотр».
- **C. Описание навыков** (отд. пункт владельца): тянуть из реальных skill `.md` (`/api/skills`), не из клиентского `const SKILLS`; правки `setSkillDesc` → PG.

## ⏳ ИДЕИ / ПРИНЦИПЫ (в конец)
- **ПРИНЦИП владельца**: ВСЕ места где объясняем/предлагаем → реальные данные и реальные интеграции (не хардкод).
- Граф связей: шторка справа «Анализ воздействия» при клике — что изменит, где агенты развёрнуты → цена/результат.

## СЛЕДУЮЩИЙ ШАГ
Полировка почти закрыта (2.1, 4.1, HITL-Предпросмотр, КАНВА-баг2 ✓). Осталось по мелочи: хвост Конверта (prod-бар/flow/audit хардкод) — но он в БД-инвентаре хардкода. → **БД-фаза** ([[persistence-localstorage-hole]]: персистентность правок в PG + сквозной снос хардкода, вкл. 7.1 биллинг / 7.2 админка). Потом кейсы #8/#9 (предзасев server-1) → Трек B ([[demo-6cases-scaling]]).

## Связанные записи
[[demo-1c-auditor]] · [[demo-6cases-scaling]] · [[ui-restructure-decisions]] · [[session-2026-09-11]] · [[server-inventory]] · [[abop-overview]] · [[abop-dev-run]]
